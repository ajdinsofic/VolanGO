export interface RequestUser {
    userId: number;          // UserId iz C# modela
    email: string;           // Email iz C# modela
    username: string;        // Username iz C# modela
    firstName: string;       // FirstName iz C# modela
    lastName: string;        // LastName iz C# modela
    gender: string;          // Gender iz C# modela
    phoneNumber: string;     // PhoneNumber iz C# modela
    dateOfBirth: Date;
    password: string       // DateOfBirth iz C# modela (DateTime)
  }
  